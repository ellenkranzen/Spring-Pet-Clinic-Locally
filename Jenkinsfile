pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/ellenkranzen/Spring-Pet-Clinic-Locally.git'
            }
        }
    }
}
